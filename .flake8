[flake8]
max-line-length = 88
# E501: line too long (handled by black)
# W503: line break before binary operator (conflicts with black's style)
ignore = E501, W503
exclude = .venv
per-file-ignores =
    # scripts use sys.path.insert() before project imports — E402 is intentional
    scripts/*.py: E402