# This configuration ensures isort sorts and formats imports in a way that is fully compatible with Black.
# It prevents conflicts between isort and Black when both tools are used in this project.
[settings]
profile = black